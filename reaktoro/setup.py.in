from distutils.core import setup

setup(name='reaktoro',
      version='${REAKTOR_VERSION}',
      package_dir={'reaktoro': 'reaktoro'},
      packages=['reaktoro', 'reaktoro.core'],
      package_data={'reaktoro': ['core/*']}
      )