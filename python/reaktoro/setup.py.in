from distutils.core import setup

setup(name='reaktoro',
      version='${REAKTORO_VERSION}',
      package_dir={'reaktoro': '${CMAKE_CURRENT_BINARY_DIR}'},
      packages=['reaktoro',
                'reaktoro.core',
                'reaktoro.demos',
                'reaktoro.flow',
                'reaktoro.interpreter',
                'reaktoro.transport',
                'reaktoro.utils'],
      package_data={'reaktoro': ['core/*']}
      )