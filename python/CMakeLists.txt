# Build the Python wrappers of Reaktoro library
add_subdirectory(PyReaktoro)

# Build the Python package for Reaktoro
add_subdirectory(reaktoro)

# Build the Reaktoro interpreter
add_subdirectory(ireaktoro)
